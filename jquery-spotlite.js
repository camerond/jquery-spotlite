(function($) {
  $.fn.spotlite = function(data, $results, options) {
    return this.each(function() {
      var defaults = {
        result_limit: 10
      };
      var opts = $.extend(defaults, options);

    });
  };
})(jQuery);
